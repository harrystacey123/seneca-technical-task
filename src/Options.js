import React, { Component } from 'react';

class Options extends Component {
    render() {
        return(
            <div>
                These are options!
            </div>
        )
    }
}

export default Options;